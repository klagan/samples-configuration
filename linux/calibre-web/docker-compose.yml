---
services:
  calibre-web:
    image: ghcr.io/linuxserver/calibre-web
    container_name: calibre-web
    environment:
      - PUID=$(id -u calibreweb)
      - PGID=$(id -g calibreweb)
      - TZ=Europe/London
      - DOCKER_MODS=linuxserver/calibre-web:calibre
    volumes:
      - /calibre/config:/config   # calibre web configuration - self generated
      - /calibre/db:/db           # metadata.db location - requires it to already exist
      - /calibre/books:/books     # where to store all books - i choose network share to separate storage and compute
    ports:
      - 8083:8083
